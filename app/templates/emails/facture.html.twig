{% block stylesheets %}
    <link
        href="https://fonts.googleapis.com/css2?family=Open+Sans:ital,wght@0,300..800;1,300..800&family=Roboto:ital,wght@0,100..900;1,100..900&family=Ubuntu+Condensed&family=Ubuntu+Sans:ital,wght@0,100..800;1,100..800&family=Ubuntu:ital,wght@0,300;0,400;0,500;0,700;1,300;1,400;1,500;1,700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css"/>
{% endblock %}
<section class="container">
    <div classs="row">
        <h1 class="text-primary">Les    {{ fees_name }}obligatoire</h1>
        <p class="text-black fs-3">Bonjour
	        <span class="fw-bolder text-uppercase">{{ parent_firstname }}</span>
	        <span class="fw-semibold text-capitalize">{{ parent_lastname }}</span>
        </p>
        <p class="fs-4">
	        pour améliorer le quotidien de vos enfants nous avons décidé de facture chaque élève
	        <strong>{{ amount }}Euros</strong> pour les <strong class="text-uppercase">{{ fees_name }}</strong>obligatoire avant la fin du mois.
        </p>
        <div class="text-center">
	        <a class="btn btn-warning" href="{{ absolute_url(path('app_payment', {id:parent_id}))}}">
		    Régulariser
	        </a>
        </div>
        <p class="fs-3">Cordialement</p>
    </div>
</section>